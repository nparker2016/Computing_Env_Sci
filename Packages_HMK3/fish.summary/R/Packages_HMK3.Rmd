---
title: "Great Lakes Fisheries"
author: "Nicol Parker"
date: "June 5, 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
source('fishcatch.R')
```

```{r}
#Q1 test data
species <- c('bluegill', 'bass', 'perch', 'trout', 'sturgeon')
price <- c( 3, 5, 3, 7, 10 )
catch_Michigan<- c(500, 100, 400, 50, 10)
catch_Superior <- c(300, 50, 400, 40, 1)
P <- as_tibble(cbind(species, price))
P$price <- as.numeric(price)
C <- as_tibble(cbind(species, catch_Michigan, catch_Superior))
C$catch_Michigan <- as.numeric(catch_Michigan)
C$catch_Superior <- as.numeric(catch_Superior)

```

```{r, results='show'}
  a <- -0.497
  b <- .1656
  c <- .08588
  d <- -0.004266
G <- cod_growth(a,b,c,d)

ggplot(G) + geom_line(aes(x=Temp_C, y=Growth_Rate))

```
